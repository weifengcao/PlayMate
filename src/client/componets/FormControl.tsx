import styled from "@emotion/styled";

export const FormControl = styled.div({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  "& input": {
    marginLeft: 20,
  },
});
